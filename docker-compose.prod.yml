version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: laravel_app
    restart: always
    working_dir: /var/www
    environment:
      APP_ENV: production
      DB_HOST: notah.cjs8m606i289.us-east-1.rds.amazonaws.com
      DB_DATABASE: notah
      DB_USERNAME: notah
      DB_PASSWORD: Gtybn!gt%025Trza
    networks:
      - app_network
   
  web:
    image: nginx:alpine
    container_name: nginx_server
    ports:
      - "80:80"
    volumes:
      - .:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - app_network

networks:
  app_network:

volumes:
  db_data:
